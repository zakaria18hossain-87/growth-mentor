import Link from 'next/link';
import {getScorecards} from '@/lib/data/scorecards';
import {averages,overall,dateLabel} from '@/lib/utils/scoring';
import {pillars,pillarLabels} from '@/lib/data/types';
export const dynamic='force-dynamic';
export default async function History(){const cards=await getScorecards();return <><div className="topline">YOUR GROWTH, OVER TIME</div><header className="page-heading"><div><h1>Every week counts.</h1><p>Look back at your progress and the reflections behind it.</p></div><Link className="button primary" href="/scorecards">Score this week ↗</Link></header>{cards.length?cards.map(c=>{const avg=averages(c.scorecard_entries);const score=overall(c.scorecard_entries);return <article className="panel history-card" key={c.id}><div className="split"><div><span className="eyebrow">WEEK OF</span><h2>{dateLabel(c.week_start)}</h2></div><div className="history-total">{score?.toFixed(1)??'—'}<small>/ 10</small></div></div><div className="history-pillars">{pillars.map(p=><div key={p}><span className={'pillar-mark '+p}/><span>{pillarLabels[p]}</span><strong>{avg[p]?.toFixed(1)??'—'}</strong></div>)}</div><details><summary>View {c.scorecard_entries.length} scores and reflections</summary>{c.scorecard_entries.map(e=><div className="history-entry" key={e.id}><div className="split"><strong>{e.goal_title}</strong><span>{e.score}/10</span></div>{e.note&&<p className="preserve">{e.note}</p>}</div>)}</details><Link className="text-link" href={'/scorecards?week='+c.week_start}>Edit this week’s scorecard ↗</Link></article>}):<div className="panel empty"><h2>No scorecards submitted yet.</h2><p>Your first reflection is the beginning of your trend.</p><Link className="button primary" href="/scorecards">Create your first scorecard</Link></div>}</>}
