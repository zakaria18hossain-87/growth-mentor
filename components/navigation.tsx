'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {useState} from 'react';
export default function Navigation(){const path=usePathname();const [open,setOpen]=useState(false);return <><header className="mobile-header"><Link href="/" className="brand">g<span>Growth Mentor</span></Link><button className="button secondary" aria-expanded={open} aria-controls="main-nav" onClick={()=>setOpen(!open)}>{open?'Close':'☰ Menu'}</button></header><aside id="main-nav" className={'sidebar '+(open?'open':'')}><Link href="/" className="brand">g<span>Growth Mentor<small>Make your future a practice.</small></span></Link><div className="nav-label">YOUR WORKSPACE</div><nav>{[['/scorecards','◫','Weekly scorecard'],['/scorecards/history','↺','History']].map(([href,icon,label])=><Link onClick={()=>setOpen(false)} aria-current={path===href?'page':undefined} className={path===href?'active':''} href={href} key={href}><span aria-hidden>{icon}</span>{label}</Link>)}</nav><div className="sidebar-bottom"><span className="demo-badge">SHARED DEMO</span><p>A space to practice growth.<br/>Changes are visible to everyone.</p><small>Private accounts come later.</small></div></aside></>}
